# Public browser token from https://account.mapbox.com/access-tokens/
# Restrict this token to the domains that serve the application.
VITE_MAPBOX_ACCESS_TOKEN=pk.replace_with_your_public_mapbox_token
